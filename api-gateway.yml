spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${JDBC_URL:jdbc:mariadb://localhost:3306/credentialsdb}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}  
